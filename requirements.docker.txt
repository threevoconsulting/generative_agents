# Minimal RUNTIME dependencies for running the simulation + environment server
# in a container. The repo's full requirements*.txt also pin heavy, analysis-only
# libraries (gensim, matplotlib, scikit-learn, statsmodels, ...) that are NOT
# imported at runtime; this curated set keeps the image small and the build
# reliable on Python 3.9 / x86.

# --- Simulation backend ---
numpy>=1.24,<2
selenium>=4.8            # imported at the top of reverie.py
anthropic>=0.39.0        # Claude (text generation)
sentence-transformers>=2.2.2   # local embeddings (pulls in torch, CPU)

# --- Environment / web server (Django + realtime) ---
Django>=2.2,<2.3
channels>=2.4,<3         # WebSocket transport (Django 2.2 compatible)
django-cors-headers>=2.5,<3.6
django-storages>=1.9,<1.13
pytz
